import {createBrowserRouter} from "react-router";
import PrivacyEN from "../pages/privacy/PrivacyEN.jsx";

const router = createBrowserRouter(
    [
        {path: "/nl", element: },
        {path: "/en", element: <PrivacyEN/>}
    ]
)